mutation AddPrice( 
	$inputs: MedicineItemInput!,
	$token: String!
) {
  addPrice( 
  inputs: $inputs,
  token: $token
  ) { 
    quantity
    details{
      name
      code
    }
    currentPrice
  }
}

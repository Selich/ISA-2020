mutation Login($email: String!, $password: String!) {
  login(inputs: { email: $email, password: $password} ) {
    errors {
      field
      message
    }
    user {
        ...RegularUser
    }
  }
}

mutation Schedule( 
  $token: String!,
  $inputs: AppointmentInput!,
) {
  schedule( 
  token: $token,
  inputs: $inputs
  ) { 
    begin
    length
    price
    patient{
      email
    }
    pharmacy{
      name
    }
  }
}

mutation ScheduleConsultation(
  $inputs: AppointmentInput!,
  $token: String!
){
  scheduleConsultation(token: $token, inputs:$inputs){
    patient{
      firstName
    }
    begin
  	length
    employee{
      firstName
      lastName
    }
  }
}
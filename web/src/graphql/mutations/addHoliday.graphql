mutation AddHoliday($from: String!, $until: String!, $employee: String!) {
  addHoliday(inputs: {
    from: $from,
    until: $until,
    employee: $employee,
  }){
    Holiday{
      id,
      from,
      until,
      employee,
      isApproved
    }
  }
}
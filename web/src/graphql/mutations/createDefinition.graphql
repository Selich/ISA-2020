mutation CreateDefinition( 
	$kind: String,
  $price: Float,
  $score: Float,
) {
  createDefinition( 
  inputs: {
    kind: $kind,
    price: $price,
    score: $score,
  }
  ) { 
		kind
    price
    score
  }
}

mutation ApproveHoliday( 
	$inputs: HolidayInput!
	$token: String!
) {
  approveHoliday(
    inputs: $inputs
    token: $token
  ) { 
    from
    until
    employee{
      firstName
      lastName
    }
}}

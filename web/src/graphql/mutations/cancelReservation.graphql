mutation CancelReservation( 
	$inputs: ReservationInput!,
	$token: String!
) {
  cancelReservation(inputs: $inputs, token: $token) {
    id
 		isBought,
    medicineItem{
      details{
        name
      }
    }
  }
}
   

query reservations( 
	$token: String!
) {
  reservations(token: $token) {
    pharmacy{
      name
    }
    patient{
      email
    }
 		isBought,
    medicineItem{
      quantity
      details{
        name
      }
    }
    deadline
  }
}

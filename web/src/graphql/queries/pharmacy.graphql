query Pharmacy(
  $inputs: PharmacyInput!
  $token: String!
) {
  pharmacy(
    inputs: $inputs
    token: $token
    ){
      id
      name
      averageRating
      address{
        street
        city
        country
      }
  }
}

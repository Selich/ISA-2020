query Employees 
(
  $inputs: EmployeeInput!
) {
  employees (
    inputs: $inputs

  ){
    id
    email,
    firstName,
    lastName,
    holidays {
      from
      until
    }
    workingHours{
      from
      until
    }
    averageRating

  }
}
